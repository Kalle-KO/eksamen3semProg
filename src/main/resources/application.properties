# ----------------------------------------------------
# Applikationsnavn
spring.application.name=Eksamensprojekt3sem

# ----------------------------------------------------
# Datasource ? skift til din MySQL-database
spring.datasource.url=jdbc:mysql://localhost:3306/siren_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=kalle
spring.datasource.password=x
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ----------------------------------------------------
# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ----------------------------------------------------
# (fjern alt der refererer til H2-console)
# ----------------------------------------------------
